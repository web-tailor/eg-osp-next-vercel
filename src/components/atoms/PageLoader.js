import IconLogoLoader from '@/components/atoms/icons/IconLogoLoader';

export default function PageLoader() {
    return (
        <div className="flex justify-center items-center h-screen w-full z-50">
            <IconLogoLoader/>
        </div>
    );
}
